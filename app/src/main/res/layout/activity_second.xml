<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">
    <!-- Action bar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_second"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:title="Sending Data - Second" />
    <!-- TextView to display the message received from MainActivity -->
    <TextView
        android:id="@+id/receivedMessage"
        android:layout_width="match_parent"
        android:layout_margin="10dp"
        android:layout_height="wrap_content"
        android:text="No message received" />

    <!-- EditText for user input to send back a reply -->
    <EditText
        android:id="@+id/editTextSecond"
        android:layout_width="match_parent"
        android:layout_margin="10dp"

        android:layout_height="wrap_content"
        android:hint="Enter reply to send" />

    <!-- Button to send reply to MainActivity -->
    <Button
        android:id="@+id/sendButtonSecond"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"

        android:backgroundTint="@color/dingley"
        android:text="Send" />
</LinearLayout>
